const N_list   = [100, 500]   # Population size list
const mu_list    = [1.0, 2, 4, 10.0]   # Mutation rate as a multiple of  1.0/N
const ngens = 1       # Generations
const burn_in= 2.0    # generations of burn_in as a multiple of N
const T     = 10000    # Number of trials
const slat_reps=-1 # slat_reps < 0 means estimate K
